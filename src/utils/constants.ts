export const GAP: number = 16;

export enum UIState {
  idle,
  loading,
  error
}

export enum RankSortOptions {
  highest = 'Highest',
  lowest = 'Lowest',
}

export enum NameSortOptions {
  az = 'A-Z',
  za = 'Z-A',
}